package eu.trentorise.smartcampus.universiadi.controller;

import org.springframework.stereotype.Controller;

import eu.trentorise.smartcampus.communicator.CommunicatorConnector;
import eu.trentorise.smartcampus.communicator.CommunicatorConnectorException;
import eu.trentorise.smartcampus.communicator.model.AppSignature;

@Controller
public class InitController {
/*
	//200219070178
	private static final String SENDER_ID = "";
	private static final String APP_ID = "universiadi2013";
	private static final String SERVER_URL = "https://vas-dev.smartcampuslab.it/core.communicator/";
	private static final String API_KEY = null;
	private static final String APP_TOKEN = null;
	CommunicatorConnector rerew;
	
	public InitController() throws CommunicatorConnectorException{
		super();
		rerew=new CommunicatorConnector(SERVER_URL, APP_ID);
		
		AppSignature appSignature=new AppSignature();
		appSignature.setApiKey(API_KEY);
		appSignature.setAppId(APP_ID);
		appSignature.setSenderId(SENDER_ID);
		
		
		rerew.registerApp(appSignature, APP_ID, APP_TOKEN);
		
		rerew.requestAppConfigurationToPush(APP_ID, APP_TOKEN);
		
	}
	*/
	
}
